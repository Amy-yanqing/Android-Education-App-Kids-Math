<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KidsMath"
        tools:targetApi="31">
        <activity
            android:name=".activities.catactivity.CatActivity"
            android:exported="false" />
        <activity
            android:name=".activities.landingactivity.LandingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.settingsactivity.SettingsActivity" />
        <activity android:name=".activities.gameactivity.GameActivity" />
        <activity android:name=".activities.gamehistoryactivity.GameHistoryActivity" />
        <activity android:name=".activities.topscoreactivity.TopScoresActivity" />
    </application>

</manifest>